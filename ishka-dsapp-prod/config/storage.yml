test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>
  host: localhost:3000

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
amazon_staging:
  service: S3
  access_key_id: <%= Rails.application.credentials[Rails.env.to_sym].dig(:aws, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials[Rails.env.to_sym].dig(:aws, :secret_access_key) %>
  region: ap-southeast-2
  bucket: ishka-ds-app-staging

amazon_prod:
  service: S3
  access_key_id: <%= Rails.application.credentials[Rails.env.to_sym].dig(:aws, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials[Rails.env.to_sym].dig(:aws, :secret_access_key) %>
  region: ap-southeast-2
  bucket: ishka-ds-app-prod

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket

# Use rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials[Rails.env.to_sym].dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
